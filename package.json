{
  "name": "@ori-serve/sip-sdk",
  "version": "0.1.0",
  "description": "Browser SDK for ORI SIP/WebRTC agent communication — handles JsSIP internally",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "jssip": "^3.10.1"
  },
  "devDependencies": {
    "@types/jssip": "^3.5.3",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
